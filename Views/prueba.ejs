<% posts.forEach(function(post, i){ %>
    <tr>
        <th scope="row"> <%= i+1 %> </th>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
     
        
    </tr>
    <% }); %>